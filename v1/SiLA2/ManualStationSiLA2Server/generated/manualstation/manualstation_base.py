# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from queue import Queue
from typing import TYPE_CHECKING, Optional, Union

from sila2.server import FeatureImplementationBase, MetadataDict, ObservableCommandInstanceWithIntermediateResponses

from .manualstation_types import (
    CancelTask_Responses,
    ConfirmTaskComplete_Responses,
    GetActiveTasks_Responses,
    GetTaskHistory_Responses,
    RequestOperatorTask_IntermediateResponses,
    RequestOperatorTask_Responses,
    SendNotification_Responses,
)

if TYPE_CHECKING:

    from typing import TypeVar

    from sila2.server import SilaServer

    Server = TypeVar("Server", bound=SilaServer)


class ManualStationBase(FeatureImplementationBase, ABC):
    parent_server: Server

    _StationStatus_producer_queue: Queue[Union[str, Exception]]
    _StationStatus_current_value: str

    _PendingTaskCount_producer_queue: Queue[Union[int, Exception]]
    _PendingTaskCount_current_value: int

    _LastNotification_producer_queue: Queue[Union[str, Exception]]
    _LastNotification_current_value: str

    RequestOperatorTask_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """

        Virtual station for operator interactions in automated workflows.
        Handles manual tasks that require human intervention, notifications,
        and confirmation of completed operations.

        Use cases:
        - Manual operations in complex workflows (e.g., "add reagent manually")
        - Tip refill notifications when Opentrons runs low
        - Quality control checkpoints
        - Sample preparation steps

        """
        super().__init__(parent_server=parent_server)

        self._StationStatus_producer_queue = Queue()

        self._PendingTaskCount_producer_queue = Queue()

        self._LastNotification_producer_queue = Queue()

        self.RequestOperatorTask_default_lifetime_of_execution = None

    def update_StationStatus(self, StationStatus: str, queue: Optional[Queue[str]] = None) -> None:
        """
        Current status of the manual station (idle, waiting_for_operator, busy).

        This method updates the observable property 'StationStatus'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._StationStatus_producer_queue
            self._StationStatus_current_value = StationStatus
        queue.put(StationStatus)

    def StationStatus_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[str]]:
        """
        Current status of the manual station (idle, waiting_for_operator, busy).

        This method is called when a client subscribes to the observable property 'StationStatus'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_StationStatus_subscriptions(self, error: Exception, queue: Optional[Queue[str]] = None) -> None:
        """
        Current status of the manual station (idle, waiting_for_operator, busy).

        This method aborts subscriptions to the observable property 'StationStatus'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._StationStatus_producer_queue
        queue.put(error)

    @property
    def current_StationStatus(self) -> str:
        try:
            return self._StationStatus_current_value
        except AttributeError:
            raise AttributeError("Observable property StationStatus has never been set")

    def update_PendingTaskCount(self, PendingTaskCount: int, queue: Optional[Queue[int]] = None) -> None:
        """
        Number of tasks waiting for operator action.

        This method updates the observable property 'PendingTaskCount'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._PendingTaskCount_producer_queue
            self._PendingTaskCount_current_value = PendingTaskCount
        queue.put(PendingTaskCount)

    def PendingTaskCount_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[int]]:
        """
        Number of tasks waiting for operator action.

        This method is called when a client subscribes to the observable property 'PendingTaskCount'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_PendingTaskCount_subscriptions(self, error: Exception, queue: Optional[Queue[int]] = None) -> None:
        """
        Number of tasks waiting for operator action.

        This method aborts subscriptions to the observable property 'PendingTaskCount'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._PendingTaskCount_producer_queue
        queue.put(error)

    @property
    def current_PendingTaskCount(self) -> int:
        try:
            return self._PendingTaskCount_current_value
        except AttributeError:
            raise AttributeError("Observable property PendingTaskCount has never been set")

    def update_LastNotification(self, LastNotification: str, queue: Optional[Queue[str]] = None) -> None:
        """
        The most recent notification message (JSON with timestamp, message, level, source).

        This method updates the observable property 'LastNotification'.

        :param queue: The queue to send updates to. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._LastNotification_producer_queue
            self._LastNotification_current_value = LastNotification
        queue.put(LastNotification)

    def LastNotification_on_subscription(self, *, metadata: MetadataDict) -> Optional[Queue[str]]:
        """
        The most recent notification message (JSON with timestamp, message, level, source).

        This method is called when a client subscribes to the observable property 'LastNotification'

        :param metadata: The SiLA Client Metadata attached to the call
        :return: Optional `Queue` that should be used for updating this property.
            If None, the default Queue will be used.
        """

    def abort_LastNotification_subscriptions(self, error: Exception, queue: Optional[Queue[str]] = None) -> None:
        """
        The most recent notification message (JSON with timestamp, message, level, source).

        This method aborts subscriptions to the observable property 'LastNotification'.

        :param error: The Exception to be sent to the subscribing client.
            If it is no DefinedExecutionError or UndefinedExecutionError, it will be wrapped in an UndefinedExecutionError.
        :param queue: The queue to abort. If None, the default Queue will be used.
        """
        if queue is None:
            queue = self._LastNotification_producer_queue
        queue.put(error)

    @property
    def current_LastNotification(self) -> str:
        try:
            return self._LastNotification_current_value
        except AttributeError:
            raise AttributeError("Observable property LastNotification has never been set")

    @abstractmethod
    def ConfirmTaskComplete(self, TaskId: str, Notes: str, *, metadata: MetadataDict) -> ConfirmTaskComplete_Responses:
        """

            Called by the operator to confirm that a manual task has been completed.
            This releases the workflow to continue.



        :param TaskId: ID of the task to confirm.

        :param Notes: Optional notes from the operator about the completed task.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ConfirmationResult: Result of the confirmation.


        """

    @abstractmethod
    def CancelTask(self, TaskId: str, Reason: str, *, metadata: MetadataDict) -> CancelTask_Responses:
        """
        Cancel a pending operator task.


        :param TaskId: ID of the task to cancel.

        :param Reason: Reason for cancellation.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - CancelResult: Result of the cancellation.


        """

    @abstractmethod
    def GetActiveTasks(self, *, metadata: MetadataDict) -> GetActiveTasks_Responses:
        """
        Get all currently active (pending or in progress) operator tasks.


        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ActiveTasks: JSON list of active tasks with their details.


        """

    @abstractmethod
    def GetTaskHistory(self, Limit: int, *, metadata: MetadataDict) -> GetTaskHistory_Responses:
        """
        Get history of completed and cancelled tasks.


        :param Limit: Maximum number of tasks to return.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - TaskHistory: JSON list of historical tasks.


        """

    @abstractmethod
    def SendNotification(
        self, Message: str, Level: str, SourceInstrument: str, *, metadata: MetadataDict
    ) -> SendNotification_Responses:
        """

            Send a notification to the operator without requiring confirmation.
            Used for informational messages.



        :param Message: Notification message text.

        :param Level: Notification level (info, warning, error).

        :param SourceInstrument: The instrument that sent this notification.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - NotificationId: Unique identifier for the notification.


        """

    @abstractmethod
    def RequestOperatorTask(
        self,
        TaskDescription: str,
        TaskType: str,
        Priority: str,
        SourceInstrument: str,
        TimeoutSeconds: int,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[RequestOperatorTask_IntermediateResponses],
    ) -> RequestOperatorTask_Responses:
        """

            Request the operator to perform a manual task.
            The workflow will wait until the operator confirms completion.
            A notification is sent to all connected clients (CLI, WebApp).



        :param TaskDescription: Description of the task for the operator to perform.

        :param TaskType: Type of task (manual_operation, tip_refill, quality_check, sample_prep, custom).

        :param Priority: Task priority (low, normal, high, urgent).

        :param SourceInstrument: The instrument that requested this task (e.g., "Opentrons Flex").

        :param TimeoutSeconds: Maximum time to wait for operator response (0 = no timeout).

        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        :return:

            - TaskId: Unique identifier for tracking this task.


        """
