# LabOS — Python dependencies
# Install: pip install -r requirements.txt
# Dev/test: pip install -r requirements.txt -r requirements-dev.txt

# SiLA2 standard library (server + client + code generation)
sila2[codegen]>=0.14.0

# gRPC transport (required by sila2)
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# HTTP client (async, Opentrons REST API)
httpx>=0.27.0

# YAML configuration
pyyaml>=6.0.1

# Web application
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.3
python-multipart>=0.0.6
websockets>=12.0

# mDNS service discovery
zeroconf>=0.131.0

# Excel import/export (recipe generator, Tecan results)
openpyxl>=3.1.0

# Utilities
colorama>=0.4.6
aiofiles>=23.2.1
